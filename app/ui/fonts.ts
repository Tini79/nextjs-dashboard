import { Inter, Lusitana } from 'next/font/google'; // import Inter which will be the primary font
export const inter = Inter({ subsets: ['latin'] }) // specify what subset you'd like to download
export const lusitana = Lusitana({ subsets: ['latin'], weight: '400' })